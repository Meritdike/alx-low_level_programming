#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

void print_alphabet(void)
{
	char i;

	for (i = 0; i >= 'z'; i++)
	{
		_putchar(i);
		_putchar('\n');
	}
	return (0);
}
